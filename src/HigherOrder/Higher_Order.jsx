import React from 'react'
export const Higher_Order = () => {
  return (
    <div className="text-capitalize text-center mt-5">
    <h1 style={{fontWeight:"bold",fontStyle:"italic"}}>it is a component that takes another component as input and returns a new 
        New component with extra features added to original component </h1>
         {/* <h1 style={{color: "red"}}>it is a component that takes another component as input and returns a new 
        New component with extra features added to original component </h1> */}
        </div>
  )
}

export default Higher_Order
